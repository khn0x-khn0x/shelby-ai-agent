# Shelby AI Agent Pipeline
- **Role:** Decentralized AI storage verification.
- **Features:** - Real-time directory watching (`watchdog`).
  - SHA-256 cryptographic hashing.
  - Environment-aware config loading (`python-dotenv`).
